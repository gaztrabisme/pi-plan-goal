# Goal — 2026-09-22 — plan-goal

**Set by:** conductor, on Gary's request, before dispatch. **Audited by:** the coordinator at Close, against this file only. Not edited after dispatch.

## Goal block

Execute plan "plan→goal bridge for pi, Claude Code, Codex" (grounded.md + Plan Block of 2026-09-22; this file is the stop). Goal rows:
1 pi probe: pi/probe.md has a SIGNAL: line naming the exact event the bridge sees when the user picks "Implement here" in @narumitw/pi-plan-mode, and a DISPATCH: yes|no line on whether sendUserMessage("/goal …", {expandPromptTemplates:true}) activates @narumitw/pi-goal
2 Claude Code probe: claude-code/probe.md has APPROVE: and REJECT: sections with the raw PostToolUse stdin for ExitPlanMode, plus the Stop hook output that blocked
3 Goal-block spec: bin/goal-block-check spec/example.md exits 0; a 4,001-char fixture exits 1; a row without a check exits 1
4 pi bridge: cd pi && npm test exits 0; bash pi/uat/headless.sh exits 0 (after approval an edit before write_goal is blocked with a reason; write_goal over 4,000 chars errors to the model; a valid block makes goal status active; then edit is allowed)
5 pi install: bash pi/uat/install.sh exits 0 (installs into a scratch PI_CODING_AGENT_DIR by path, pi -p "hi" starts with no extension load error)
6 Claude Code plugin: bash claude-code/uat.sh exits 0 (PostToolUse fixture → additionalContext names the goal file; Stop with a 4,001-char goal → exit 2; Stop with a valid goal → systemMessage contains "/goal Execute plan"; PLAN_GOAL_LOOP=1 stops after 8 continuations)
7 Codex: bash codex/uat.sh exits 0 (Stop fixture with plan flag and no goal → decision block once, second call passes; codex/skill/SKILL.md has name and description frontmatter)
8 README: install line per built harness (grep -c 'pi install\|hooks.json\|codex' README.md ≥ 3) and a table with one row each for Gemini, Copilot, Cursor, opencode, Oh My Pi naming the seam
9 Review: research/review.md exists; every HIGH finding has a fix commit hash or a rejection line in wiki/decisions.md
10 Wiki: wiki/log.md carries the run ledger (unit, lane, verdict, acceptance result, forks decided forward, lane refusals); wiki/decisions.md has D1–D10 with rejected options; wiki/active-work.md reconciles every unit as done, deferred or blocked
11 Repo: git log shows ≥ 1 commit per wave; git status is clean at Close

## Working pattern (binds this run)
U0a, U0b: Claude agents (they need pi and claude locally). U1 pi bridge: Codex gpt-5.3-codex-spark, fallback Claude agent. U2, U3, U4, U6: GLM. U5 review: DeepSeek, fallback Codex gpt-6-astra. Lanes live at dispatch: GLM, DeepSeek (reconnected 2026-09-22), Claude agent; Codex credit untested. Coordinator runs every acceptance itself.
